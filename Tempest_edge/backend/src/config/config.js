// Central configuration file for the application.
// For now, we are using .env, but for complex apps, this is a good practice.
const config = {
    // API_VERSION: 'v1'
    // Database connection strings
    // Feature flags
};

module.exports = config;