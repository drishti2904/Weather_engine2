const express = require('express');
const router = express.Router();
const voyageController = require('./voyage.controller.js');

// No specific routes defined yet, but this file is a placeholder for future growth.
// For now, the main logic is handled under the vessel routes.
// Example: router.post('/compare-routes', voyageController.compareRoutes);

module.exports = router;